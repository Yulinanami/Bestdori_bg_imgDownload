# Bestdori 背景图下载（简易版）

不需要浏览器内核，开箱即用。

## 依赖

```bash
pip install -r requirements.txt
```

## 使用

```bash
python main.py
```

运行后按提示输入：

1) 起始/结束 scenario 编号（默认 0-123，可交换顺序）。  
2) 是否按 scenario 分目录保存（默认否，输入 Y/y 开启）。  
3) 输出目录。

或者直接一路回车即可。
